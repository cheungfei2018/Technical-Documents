1.RegExp的构造函数
	es5:
		1.参数1是字符串，参数2是正则表达式的修饰符(i\g等)
		2.参数是一个正则表达式(这时返回正则表达式的拷贝)，这时不允许使用第二个参数添加修饰符，否则报错
	
	es6：
		改变了上面2中的行为，如果第二个参数添加修饰符，则会忽略掉第一个参数中正则表达式的修饰符，而使用第二个参数
		
2.u修饰符
	es6添加了u修饰符，意为‘unicode模式’，用来正确处理大于\uffff的Unicode字符，即可以处理UTF-16编码(ES5不支持)
	