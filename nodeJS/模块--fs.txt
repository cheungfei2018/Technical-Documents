fs：文件系统；是对标准POSIX函数的简单封装

使用
const fs = require('fs');

tip:
	1.fs的所有方法都有异步和同步的模式
	2.异步方法的最后一个参数都是回调函数，回调函数的参数取决于具体方法，但是回调函数的第一个参数都会保留给异常，如果操作成功，第一个参数为null或者undefined
	3.当使用同步方法是，任何异常都会立即抛出，可以使用try/catch来处理异常，或让异常向上冒泡
	4.在繁忙的进程中推荐使用异步方法。